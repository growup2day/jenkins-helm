JCasC:
  quietDown:
    # cs2 cluster contains the backup instance, in quiet down mode by default
    quietDown: true

  openshiftClouds:
    dev:
      serverUrl: "https://api.cs2-dev.nz.service.test:6443"
      credentialsId: "cs2-dev-openshift"

  permanentNodes:
    agents:
      nz31rmpa001v:
        nodeDescription: "Building Java 11 apps on RHEL8 nz31rmpa001v"
        remoteFS: "/app/jenkins/cs2-jenkins-agent"
        labelString: "jenkins-java11-build-env"
