should include blank proxy config in default config scripts when not configured:
  1: |
    |-
      jenkins:
        # Manage Plugins > Advanced
        proxy:
should include proxy config in default config scripts when configured:
  1: |
    |-
      jenkins:
        # Manage Plugins > Advanced
        proxy:
          name: 10.1.1.1
          noProxyHost: '*.service.dev'
          port: 80
