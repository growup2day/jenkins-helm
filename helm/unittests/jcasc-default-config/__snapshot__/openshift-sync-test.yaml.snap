should disable OpenShift sync in default config script when not configured:
  1: |
    |-
      unclassified:
        # Configure System > OpenShift Jenkins Sync
        globalPluginConfiguration:
          enabled: false
should include OpenShift sync in default config script when configured:
  1: |
    |-
      unclassified:
        # Configure System > OpenShift Jenkins Sync
        globalPluginConfiguration:
          enabled: true
